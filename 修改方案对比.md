# 修改方案对比

## 🎯 问题根源

节点A故障后，其他节点等A，因为主循环在检查 `currentDelegate == keyAddr`，而这个地址依赖过时的 `currentDelegateIndex`。

## 💡 两个解决方案

### 方案1：简化主循环（推荐⭐）

**核心思想**：既然 `shouldProduceBlockNow()` 已经正确判断了，主循环就相信它。

**修改位置**：`dpos.go` 第1035-1166行

**修改后的代码**：
```go
if shouldProduce {
    // TRON方式：直接相信 shouldProduceBlockNow() 的结果
    r.logger.Info("✅ shouldProduceBlockNow返回true，开始出块")
    if err := r.produceBlock(); err != nil {
        r.logger.Error("出块失败", "error", err)
    }
}
```

**删除**：所有地址比较的代码（1037-1166行，共130行）

**优点**：
- ✅ 修改最小（删除130行，添加2行）
- ✅ 风险最低
- ✅ 完全符合Tron方式
- ✅ 不需要改其他函数

---

### 方案2：修改 getCurrentDelegate() 基于时间计算

**核心思想**：让 `getCurrentDelegate()` 也基于时间实时计算索引。

**修改位置**：`dpos.go` 第2316行

**修改内容**：
```go
// 在这段代码之后（第2334行之后）
if actualDelegateCount == 0 {
    r.logger.Warn("🔍 getCurrentDelegate: 数据库中没有验证者")
    return types.ZeroAddress
}

// 🆕 添加：基于时间实时计算索引（Tron方式）
var currentDelegateIndex uint64
if r.config != nil && r.config.blockScheduler != nil && !r.config.blockScheduler.GetGenesisTime().IsZero() {
    now := time.Now()
    genesisTime := r.config.blockScheduler.GetGenesisTime()
    blockWindow := r.config.blockScheduler.GetBlockWindow()
    timeSinceGenesis := now.Sub(genesisTime)
    currentSlot := int(timeSinceGenesis / blockWindow)
    currentDelegateIndex = uint64(currentSlot % actualDelegateCount)
} else {
    // 回退到缓存索引
    currentDelegateIndex = r.currentDelegateIndex
}

// 替换 r.currentDelegateIndex 为 currentDelegateIndex（第2355、2363行）
```

**优点**：
- ✅ 更符合Tron的时间驱动方式
- ✅ 修复彻底
- ✅ 不影响现有调用逻辑

**缺点**：
- ⚠️ 需要修改一个完整的函数
- ⚠️ 有性能影响（每次调用都计算时间）

---

## 📊 推荐方案1的理由

1. **修改最小**：只需改主循环
2. **风险最低**：不涉及其他函数
3. **完全有效**：移除了冗余检查
4. **符合Tron**：直接相信时间驱动的判断

## 🚀 实施建议

**推荐方案1（简化主循环）**：
```go
// 修改 continuousBlockMonitoring() 函数
if shouldProduce {
    r.logger.Info("✅ 轮到当前节点出块", "timestamp", time.Now().Format("15:04:05.000"))
    if err := r.produceBlock(); err != nil {
        r.logger.Error("出块失败", "error", err)
    }
} else {
    // 不需要出块
}
```

**删除从第1037行到第1166行的所有代码**

