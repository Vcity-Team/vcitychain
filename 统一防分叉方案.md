# 统一防分叉方案

## 🎯 核心原则

**只信任 `shouldProduceBlockNow()` 的实时时间判断！**

## 🔍 处理的场景

### 场景1：节点超时（构建耗时>2秒）

```
10:00:00: A的slot开始，shouldProduceBlockNow() = true
10:00:00-10:00:03: A在buildBlock()中（耗时3秒）
10:00:02: A的slot时间窗口结束
10:00:05: A完成buildBlock()，尝试提交
```

**处理**：提交前再次调用 `shouldProduceBlockNow()`，返回false → 丢弃

### 场景2：节点被跳过

```
10:00:00: A的slot开始，shouldProduceBlockNow() = true
10:00:00-10:00:03: A在buildBlock()中
10:00:02: B开始出块并提交成功（A被跳过）
10:00:05: A完成buildBlock()，尝试提交
```

**处理**：父区块已变化 → 丢弃

### 场景3：多个节点竞争同一slot

```
10:00:00: A和B都收到slot通知
10:00:01: A先完成并提交
10:00:02: B完成并尝试提交
```

**处理**：第830行WriteFullBlock检查 → 区块已存在 → 丢弃

---

## ✅ 解决方案

### 修改点1：在 CommitBlock 前再次验证（已有）

**位置**：`consensus/dpos/dpos.go` 第1487-1511行

**作用**：
1. 检查时间窗口是否仍有效
2. 检查父区块是否已变化

**已实现**：✅

### 修改点2：简化主循环，移除冗余检查

**位置**：`consensus/dpos/dpos.go` 第1427-1437行（原检查currentDelegate的代码）

**当前代码**：
```go
if currentDelegate != keyAddr {
    return nil // 不是当前出块者
}

if !r.shouldProduceBlock() {
    return nil
}
```

**应该改为**：
```go
if !r.shouldProduceBlock() {
    return nil
}
// 移除 currentDelegate == keyAddr 检查
```

**原因**：
- `shouldProduceBlock()` 内部已经调用了 `shouldProduceBlockNow()`
- `shouldProduceBlockNow()` 基于实时时间计算
- 不需要再检查过时的 `currentDelegateIndex`

---

## 📋 完整修改清单

### 1. 保持第1487-1511行（已有）

```go
// 🆕 TRON方式：构建完成后再次检查时间窗口和父区块
if r.config.blockScheduler != nil {
    currentBlock := r.config.blockchain.CurrentHeader()
    
    // 检查1：时间窗口是否仍有效
    if !r.config.blockScheduler.ShouldProduceBlockNow(
        int(r.currentDelegateIndex),
        currentBlock.Number,
    ) {
        return nil
    }
    
    // 检查2：父区块是否已变化
    if currentBlock.Hash != block.Block.Header.ParentHash {
        return nil
    }
}
```

### 2. 简化 produceBlock() 开始部分（建议）

**当前**（第1427-1448行）：
```go
// 获取当前区块和当前委托者
currentBlock := r.config.blockchain.CurrentHeader()
currentDelegate := r.getCurrentDelegate()

// 检查是否应该产生区块
if currentDelegate != keyAddr {
    return nil
}

if !r.shouldProduceBlock() {
    return nil
}
```

**应该改为**：
```go
// 获取当前区块（用于后续检查）
currentBlock := r.config.blockchain.CurrentHeader()

// 🎯 只依赖 shouldProduceBlock() 的实时判断
if !r.shouldProduceBlock() {
    return nil
}
```

**删除代码**：
```go
// ❌ 删除这些
currentDelegate := r.getCurrentDelegate()
if currentDelegate != keyAddr {
    return nil
}
```

---

## 🎯 核心修改点

### 修改1：简化 produceBlock() 开始

**文件**：`consensus/dpos/dpos.go`  
**位置**：第1427-1448行  
**修改**：移除 `currentDelegate == keyAddr` 检查

### 修改2：保持提交前检查（已有）

**文件**：`consensus/dpos/dpos.go`  
**位置**：第1487-1511行  
**修改**：无需修改（已有）

---

## 🔒 防护机制

### 三层防护

1. **开始前检查**（produceBlock 开始）：
   - `shouldProduceBlock()` → 调用 `shouldProduceBlockNow()`
   - 基于实时时间判断

2. **提交前检查**（buildBlock 完成后）：
   - 再次调用 `shouldProduceBlockNow()`
   - 检查父区块是否变化

3. **写入时检查**（WriteFullBlock）：
   - 检查区块号是否已存在

---

## ✅ 处理所有场景

### 场景A：节点超时（buildBlock耗时>2秒）

```
10:00:00: shouldProduceBlockNow() = true ✅
10:00:00-10:00:03: buildBlock()中
10:00:03: 完成，提交前检查
         → shouldProduceBlockNow() = false ❌
         → 丢弃
```

### 场景B：节点被跳过

```
10:00:00: shouldProduceBlockNow() = true ✅
10:00:00-10:00:03: buildBlock()中
10:00:02: 其他节点出块
10:00:03: 完成，提交前检查
         → 父区块已变化
         → 丢弃
```

### 场景C：多个节点竞争

```
10:00:00: A和B都 shouldProduceBlockNow() = true
10:00:01: A提交成功
10:00:02: B提交
         → WriteFullBlock 检查
         → 区块已存在
         → 丢弃
```

---

## 🎉 总结

### 核心原则
**只信任 shouldProduceBlockNow() 的实时判断！**

### 关键修改
1. **删除**：`currentDelegate == keyAddr` 检查
2. **保持**：`shouldProduceBlock()` 检查
3. **保持**：提交前再次验证（第1487-1511行）
4. **保持**：WriteFullBlock 检查

### 为什么不会差？
- Tracker 只在区块写入后才记录
- 任何被丢弃的区块都不会写入
- Tracker 和区块链完全一致

