# 网络层改进说明

## 问题分析

经过深入分析，发现状态广播丢失的根本原因是：

1. **网络缓冲区溢出**：libp2p的缓冲区大小设置为1024，当网络负载增加时，缓冲区满导致消息丢失
2. **缺乏重试机制**：广播失败时没有重试，直接丢失消息
3. **网络状态监控不足**：无法及时发现网络问题
4. **并发风暴导致的文件系统瓶颈**：大量签名请求同时到达，导致文件读取阻塞和goroutine爆炸

## 解决方案

### 1. 增加网络缓冲区大小

- `peerOutboundBufferSize`: 1024 → 8192 (8倍)
- `validateBufferSize`: 1024 → 8192 (8倍)  
- `subscribeOutputBufferSize`: 1024 → 8192 (8倍)

### 2. 添加重试机制

- 状态广播失败时自动重试3次
- 每次重试间隔100ms
- 避免因临时网络问题导致的消息丢失

### 3. 网络健康检查

- 每30秒检查一次网络状态
- 监控连接节点数量
- 检查Topic状态
- 记录网络异常情况

### 4. 网络性能优化

- 保持默认的GossipSub参数以确保稳定性
- 通过增加缓冲区大小来提升性能
- 避免自定义参数导致的运行时错误

### 5. 增强日志记录

- 详细的网络错误信息
- 网络状态统计
- 缓冲区状态监控
- 连接状态诊断

### 6. 解决并发风暴问题

- **私钥缓存机制**：避免重复文件读取，减少文件系统竞争
- **并发控制**：限制同时处理的签名请求数量（最多10个）
- **信号量机制**：防止goroutine数量爆炸
- **异步处理**：将签名请求处理放在独立的goroutine中

## 使用方法

重新编译并运行程序，改进将自动生效：

```bash
go build -o vcitychain
./vcitychain server
```

## 监控指标

程序运行后，可以通过日志监控以下指标：

1. **网络连接数**：每30秒记录一次
2. **状态广播成功率**：包含重试次数和最终结果
3. **网络缓冲区状态**：自动检测缓冲区满的情况
4. **网络健康状态**：连接异常时发出警告

## 预期效果

- 解决区块26状态广播丢失问题
- 提高网络消息传播的可靠性
- 减少因网络缓冲区满导致的消息丢失
- 提供更好的网络问题诊断能力
- 解决并发风暴和goroutine爆炸问题
- 显著提升系统稳定性和性能

## 注意事项

1. 增加缓冲区大小会占用更多内存
2. 重试机制会增加网络负载
3. 建议在生产环境中监控内存使用情况
