# 恢复基于索引的方案

## 方案概述

将当前基于**时间slot**的TRON方案改回基于**区块号索引**的方案。

### 当前方案（基于时间）
- 计算：`currentSlot = timeSinceGenesis / blockWindow`
- 索引：`expectedValidatorIndex = currentSlot % len(validators)`
- 优点：与TRON完全一致，基于绝对时间
- 缺点：时间不同步会导致出块不一致

### 恢复方案（基于索引）
- 计算：`expectedValidatorIndex = currentBlockNumber % len(validators)`
- 索引：基于区块号计算，而不是时间
- 优点：逻辑简单，不依赖时间同步
- 缺点：需要维护 currentDelegateIndex

## 修改点列表

### 1. 恢复 currentDelegateIndex 字段

**文件**：`consensus/dpos/dpos.go`

**修改位置**：第330-360行的 `dposRuntime` 结构体

```go
type dposRuntime struct {
	config  *runtimeConfig
	backend dposBackend
	logger  hclog.Logger

	// 网络服务
	network *network.Server

	// 网络主题缓存
	signatureRequestTopic  *network.Topic
	signatureResponseTopic *network.Topic
	signatureQueryTopic    *network.Topic
	topicMutex             sync.RWMutex

	// 运行时状态
	currentRound           uint64
	lastProducedSlot       int // 🆕 记录上次出块的 slot，防止一个 slot 内出多个区块
	currentDelegateIndex   uint64 // ✅ 恢复：基于区块号的委托者索引
	delegates              validator.AccountSet
	voters                 map[types.Address]*VoterInfo
	pendingVotes           []*VoteMessage

	// ... 其他字段
}
```

### 2. 修改 block_scheduler.go 添加基于区块号的方法

**文件**：`consensus/dpos/block_scheduler.go`

**新增方法**：`ShouldProduceBlockByIndex`

```go
// ShouldProduceBlockByIndex 基于区块号索引的出块检查（恢复原方案）
func (bs *BlockScheduler) ShouldProduceBlockByIndex(validatorAddress types.Address, validators []types.Address, currentBlockNumber uint64, validatorIndex uint64) bool {
	bs.mutex.RLock()
	defer bs.mutex.RUnlock()

	if len(validators) == 0 {
		bs.logger.Warn("⚠️ 验证者列表为空")
		return false
	}

	// 基于区块号计算应该出块的验证者索引
	expectedValidatorIndex := int(currentBlockNumber) % len(validators)
	expectedValidator := validators[expectedValidatorIndex]

	// 检查是否轮到自己
	if validatorIndex != uint64(expectedValidatorIndex) {
		// 防刷屏：每10秒打印一次日志
		if time.Now().Sub(bs.lastLogTime) >= 10*time.Second {
			bs.logger.Info("⏭️ 不是当前轮次的验证者（基于索引）",
				"myIndex", validatorIndex,
				"expectedIndex", expectedValidatorIndex,
				"currentBlockNumber", currentBlockNumber,
				"validatorCount", len(validators),
				"action", "跳过出块",
				"timestamp", time.Now().Format("15:04:05.000"))
			bs.lastLogTime = time.Now()
		}
		return false
	}

	// 检查是否是我的地址
	if validatorAddress != expectedValidator {
		if time.Now().Sub(bs.lastLogTime) >= 10*time.Second {
			bs.logger.Info("⏭️ 地址不匹配（基于索引）",
				"myAddress", validatorAddress.String(),
				"expectedValidator", expectedValidator.String(),
				"currentBlockNumber", currentBlockNumber,
				"validatorIndex", validatorIndex,
				"expectedIndex", expectedValidatorIndex,
				"action", "跳过出块")
			bs.lastLogTime = time.Now()
		}
		return false
	}

	// 可以出块
	if time.Now().Sub(bs.lastLogTime) >= 5*time.Second {
		bs.logger.Info("✅ 基于索引的出块时机到达",
			"validatorIndex", validatorIndex,
			"validatorAddress", validatorAddress.String(),
			"currentBlockNumber", currentBlockNumber,
			"expectedIndex", expectedValidatorIndex)
		bs.lastLogTime = time.Now()
	}
	return true
}
```

### 3. 修改 updateRound 方法

**文件**：`consensus/dpos/dpos.go`

**修改位置**：第1736行的 `updateRound` 方法

```go
// updateRound 更新轮次
func (r *dposRuntime) updateRound(blockNumber ...uint64) {
	var currentBlockNumber uint64

	if len(blockNumber) > 0 {
		currentBlockNumber = blockNumber[0]
	} else {
		if r.config != nil && r.config.blockchain != nil {
			if currentHeader := r.config.blockchain.CurrentHeader(); currentHeader != nil {
				currentBlockNumber = currentHeader.Number
			}
		}
	}

	// ✅ 恢复：重新计算并设置 currentDelegateIndex
	if len(r.delegates) > 0 {
		r.currentDelegateIndex = uint64(currentBlockNumber) % uint64(len(r.delegates))
		r.logger.Info("🔄 更新当前委托者索引",
			"blockNumber", currentBlockNumber,
			"delegateCount", len(r.delegates),
			"currentDelegateIndex", r.currentDelegateIndex,
			"currentRound", r.currentRound)
	}

	// 更新轮次
	if r.config != nil && r.config.DelegateCount > 0 {
		r.currentRound = uint64(currentBlockNumber) / uint64(r.config.DelegateCount)
		r.logger.Debug("🔄 更新轮次",
			"currentRound", r.currentRound,
			"currentBlockNumber", currentBlockNumber,
			"delegateCount", r.config.DelegateCount)
	}
}
```

### 4. 修改 shouldProduceBlock 方法

**文件**：`consensus/dpos/dpos.go`

**修改位置**：第1985行的 `shouldProduceBlock` 方法

```go
// shouldProduceBlock 检查当前节点是否应该出块（基于区块号索引）
func (r *dposRuntime) shouldProduceBlock() bool {
	currentBlock := r.config.blockchain.CurrentHeader()
	if currentBlock == nil {
		r.logger.Warn("无法获取当前区块头，跳过出块")
		return false
	}

	// ✅ 恢复：使用基于区块号的索引方案
	myAddress := types.Address(r.config.Key.Address())
	myValidatorIndex := r.getMyValidatorIndex(myAddress)
	
	if myValidatorIndex == -1 {
		r.logger.Warn("⚠️ 当前节点不在验证者列表中")
		return false
	}

	validators := make([]types.Address, len(r.delegates))
	for i, d := range r.delegates {
		validators[i] = d.Address
	}

	// 使用基于索引的方法
	if r.config.blockScheduler != nil {
		return r.config.blockScheduler.ShouldProduceBlockByIndex(
			myAddress, 
			validators, 
			currentBlock.Number,
			uint64(myValidatorIndex))
	}

	return false
}

// ✅ 新增：获取当前节点在验证者列表中的索引
func (r *dposRuntime) getMyValidatorIndex(myAddress types.Address) int {
	for i, d := range r.delegates {
		if d.Address == myAddress {
			return i
		}
	}
	return -1
}
```

### 5. 修改 shouldProduceBlockNow 方法

**文件**：`consensus/dpos/dpos.go`

**修改位置**：第1077行的 `shouldProduceBlockNow` 方法

```go
func (r *dposRuntime) shouldProduceBlockNow() bool {
	currentBlock := r.config.blockchain.CurrentHeader()
	if currentBlock == nil {
		r.logOnceWithInterval("should_produce_block_now_no_header", 10*time.Second, "warn",
			"❌ shouldProduceBlockNow: 无法获取当前区块头",
			"timestamp", time.Now().Format("15:04:05.000"))
		return false
	}

	// ✅ 恢复：优先使用基于索引的方案
	myAddress := types.Address(r.config.Key.Address())
	myValidatorIndex := r.getMyValidatorIndex(myAddress)
	
	if myValidatorIndex == -1 {
		r.logOnceWithInterval("should_produce_block_now_not_in_validators", 10*time.Second, "warn",
			"⚠️ 当前节点不在验证者列表中",
			"myAddress", myAddress.String())
		return false
	}

	validators := make([]types.Address, len(r.delegates))
	for i, d := range r.delegates {
		validators[i] = d.Address
	}

	// ✅ 使用基于索引的方法，而不是基于时间的方法
	if r.config.blockScheduler != nil {
		return r.config.blockScheduler.ShouldProduceBlockByIndex(
			myAddress, 
			validators, 
			currentBlock.Number,
			uint64(myValidatorIndex))
	}

	return false
}
```

### 6. 修改 GetCurrentDelegate 方法

**文件**：`consensus/dpos/dpos.go`

**修改位置**：第7424行的 `GetCurrentDelegate` 方法

```go
func (d *DPoS) GetCurrentDelegate() types.Address {
	// ✅ 恢复：使用 currentDelegateIndex
	if d.runtime != nil && d.runtime.currentDelegateIndex < uint64(len(d.runtime.delegates)) {
		return d.runtime.delegates[d.runtime.currentDelegateIndex].Address
	}
	return types.ZeroAddress
}
```

## 修改影响分析

### 优点
1. ✅ 逻辑简单：直接基于区块号计算，不需要时间同步
2. ✅ 确定性：所有节点基于区块号计算，结果一致
3. ✅ 无时间依赖：不依赖时间精度，避免时钟偏移问题

### 缺点
1. ⚠️ 需要维护索引：需要每个区块更新 currentDelegateIndex
2. ⚠️ 需要状态管理：需要跟踪和管理验证者列表的索引

### 关键差异

| 对比项 | 基于时间的方案（当前） | 基于索引的方案（恢复） |
|-------|----------------------|---------------------|
| 计算方式 | `currentSlot = time % blockWindow` | `index = blockNumber % validators` |
| 时间依赖 | ✅ 需要时间同步 | ❌ 不依赖时间 |
| 确定性 | ⚠️ 依赖时间精度 | ✅ 完全确定 |
| 复杂度 | 低 | 中 |

## 实施步骤

1. **第一步**：恢复 `currentDelegateIndex` 字段
2. **第二步**：添加 `ShouldProduceBlockByIndex` 方法
3. **第三步**：修改 `updateRound` 方法
4. **第四步**：修改 `shouldProduceBlock` 方法
5. **第五步**：修改 `shouldProduceBlockNow` 方法
6. **第六步**：修改 `GetCurrentDelegate` 方法
7. **第七步**：测试验证

## 注意事项

1. ⚠️ 需要确保每个区块都正确更新 `currentDelegateIndex`
2. ⚠️ 需要确保验证者列表顺序与所有节点一致
3. ⚠️ 需要处理共识切换时的索引计算
4. ⚠️ 需要测试验证者变化时的索引更新

