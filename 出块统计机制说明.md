# 出块统计机制说明

## 📊 两种统计机制

### 1️⃣ Tracker记录（用于奖励）

**位置**：`consensus/dpos/dpos.go` 第14585行

**何时记录**：
```go
// 第14567-14600行
func (d *DPoS) processEconomicSystem(block *types.FullBlock) error {
    blockNumber := block.Block.Number()
    blockTime := time.Unix(int64(block.Block.Header.Timestamp), 0)
    blockProducer := types.BytesToAddress(block.Block.Header.Miner)
    currentEpoch := d.epochManager.GetCurrentEpoch(blockNumber)
    
    // 🎯 每次出块后立即记录
    d.blockTracker.RecordBlockProduction(
        blockNumber,
        blockTime,
        blockProducer,
        currentEpoch,
    )
    
    return nil
}
```

**实现**：`consensus/dpos/block_production_tracker.go` 第89-144行

```go
func (bpt *BlockProductionTracker) RecordBlockProduction(
    blockNumber uint64,
    blockTime time.Time,
    producer types.Address,
    epochNumber uint64,
) {
    // 记录出块
    bpt.currentEpochBlocks[producer]++  // ← 第134行
    
    // 记录区块时间
    bpt.currentEpochBlockTimes[producer] = append(
        bpt.currentEpochBlockTimes[producer], 
        blockTime
    )
}
```

**存储**：
- **内存**：`currentEpochBlocks map[types.Address]uint64`
- **数据库**：epoch变化时保存到DB

**用途**：✅ 用于奖励分配

---

### 2️⃣ 区块链查询（用于故障检测）

**位置**：`consensus/dpos/dpos.go` 第15971-15987行

**何时统计**：Epoch结束时调用 `calculateMissedBlocks()`

```go
// 第15971-15987行
func (d *DPoS) calculateMissedBlocks(validatorAddr types.Address, startEpoch, endEpoch uint64) uint64 {
    // ...
    
    // 🎯 直接查询区块链历史
    for blockNum := epochStartBlock; blockNum < epochEndBlock; blockNum++ {
        if header, exists := d.blockchain.GetHeaderByNumber(blockNum); exists {
            // 检查 Miner 字段
            if len(header.Miner) == 20 {
                minerAddr := types.Address(header.Miner)
                if minerAddr == validatorAddr {
                    actualBlocks++  // ← 第15977行
                }
            }
        }
    }
    
    // 计算漏块数
    if expectedBlocks > actualBlocks {
        missedBlocks = expectedBlocks - actualBlocks
    }
    
    return missedBlocks
}
```

**数据源**：
- **查询区块链**：`d.blockchain.GetHeaderByNumber()`
- **检查字段**：`header.Miner`
- **不依赖Tracker**：直接从区块链读取

**用途**：✅ 用于故障检测（标记故障节点）

---

## 🔍 关键区别

### Tracker记录
- 每次出块后立即调用（第14585行）
- 存储到内存 + 数据库
- 即使节点被移除、重启也能保留
- 用于奖励分配

### 区块链查询
- Epoch结束时才统计（第15971行）
- **只统计成功写入区块链的区块**
- 被丢弃的区块不会计入
- 用于故障检测

---

## 🎯 你的场景分析

### 场景：节点A超时，区块被丢弃

```
10:00:00: A的slot开始
10:00:03: A完成 buildBlock()
10:00:05: A尝试 CommitBlock()
         → 第1500行：时间窗口检查失败
         → 第1500行：return nil，不提交

结果：
- A的区块**没有写入区块链**
- Tracker**不会记录**（因为第14585行是在processEconomicSystem中，区块必须先写入）
- 区块链查询**找不到**A的区块
```

### 统计时

```go
// 第15971行查询区块链
for blockNum := epochStartBlock; blockNum < epochEndBlock; blockNum++ {
    if header, exists := d.blockchain.GetHeaderByNumber(blockNum); exists {
        if header.Miner == validatorAddr {
            actualBlocks++  // ← 找不到A的区块
        }
    }
}

// 计算漏块
missedBlocks = expectedBlocks - actualBlocks  // ← A被记为漏块
```

---

## 📊 两套机制的原因

### 为什么需要两套？

1. **Tracker记录**：
   - 快速访问
   - 用于奖励分配
   - 即使区块被回滚也能记录
   
2. **区块链查询**：
   - 权威数据源
   - 真实记录
   - 用于故障检测

### 关键点

**漏块统计只看区块链，不看Tracker！**

这意味着：
- ✅ A的区块被丢弃（第1500行）
- ✅ 区块链上没有A的区块记录
- ✅ Epoch结束统计时（第15971行）
- ✅ **找不到A的区块，记为漏块**

---

## 🎉 总结

### 统计实际出块的位置

| 机制 | 位置 | 数据源 | 何时记录 | 用途 |
|------|------|--------|---------|------|
| **Tracker记录** | `dpos.go:14585`<br>`block_production_tracker.go:134` | 内存+DB | 每次出块后 | 奖励分配 |
| **区块链查询** | `dpos.go:15971` | 区块链 | Epoch结束时 | 故障检测 |

### 你的问题

> "不是每次出块后 tracker记录么"

**回答**：
1. ✅ Tracker确实记录（第14585行）
2. ✅ 但**漏块统计不查Tracker**！
3. ✅ 漏块统计直接查区块链（第15971行）
4. ✅ 如果区块没写入区块链，就是漏块

### 关键代码

**Tracker记录**：
```go
// 第14585行：processEconomicSystem()
d.blockTracker.RecordBlockProduction(...)

// 第134行：block_production_tracker.go
bpt.currentEpochBlocks[producer]++
```

**漏块统计**：
```go
// 第15971行：calculateMissedBlocks()
for blockNum := epochStartBlock; blockNum < epochEndBlock; blockNum++ {
    header := d.blockchain.GetHeaderByNumber(blockNum)
    if header.Miner == validatorAddr {
        actualBlocks++  // ← 查的是区块链
    }
}
```

**结论：漏块统计只看区块链！** 🎯

